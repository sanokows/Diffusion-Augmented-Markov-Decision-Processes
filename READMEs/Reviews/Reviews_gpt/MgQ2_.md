We thank the reviewer for the thoughtful and constructive feedback. Below we address the main concerns:

**A1: W3, Q1, Q3 - Why introduce the diffusion MDP at all? Could this be avoided?**  
Yes, diffusion-RL objectives can be derived without explicitly defining an augmented MDP, as in DIME (see [link](https://github.com/dmerlicml/DMERL_Rebuttal/blob/main/Table/DIMEvsDMERL.pdf) for a one-to-one comparison to DME; equations there are denoted T-Eq. X). However, that route leads to objectives over the **entire reverse diffusion chain**; see T-Eqs. (1), (5), and (10). In contrast, our diffusion-MDP formulation yields a **step-wise objective**: optimization is performed at sampled diffusion steps using a diffusion-aware value function, without backpropagating through the full reverse chain; see T-Eqs. (3), (7), and (11). This is why diffusion steps are part of the MDP state. The diffusion-MDP is therefore not just a reformulation; it enables a novel, more flexible RL objective. We agree this should be stated more clearly and will revise accordingly. See also A2 to reviewer k9mC.

**A2: W2 - Is the contribution mainly a unified formulation?**  
We respectfully disagree. The **maximum-entropy diffusion MDP** itself (Eqs. 11–13) is, to our knowledge, new and provides a principled recipe for deriving **diffusion-policy counterparts of max-ent RL algorithms**. From it, we derive **DME-PPO, DME-REPPO, and DA-MDP WPO**, all of which are new. As stated in L.077ff, the only direct connection to prior work is that **DPPO emerges as the zero-temperature special case of DME-PPO**. Thus, the paper does not simply unify existing methods; it introduces a new max-ent formulation and new algorithms derived from it.

**A3: W3, Q4 - Exploration and multimodality .**  
We added a toy example ([link](https://github.com/dmerlicml/DMERL_Rebuttal/blob/main/MultimodalActions/multi_agents.gif)) showing that our methods can learn **multimodal action distributions**. A 2D agent outputs actions in $[-1,1]$, corresponding to directions between $-90^\circ$ and $90^\circ$. Rewards are given by a double-well potential with optima at $-45^\circ$ and $45^\circ$. The state is the agent’s orientation. The transition maps the chosen action to the nearest global maximum, while the reward is computed from the original action. This yields only 8 states, allowing us to visualize, for each state, the learned action histogram ([link](https://github.com/dmerlicml/DMERL_Rebuttal/blob/main/MultimodalActions/histo.png)) alongside the reward landscape.

We compare **REPPO, DIME-REPPO, DPPO, DME-REPPO, DA-MDP WPO, and DME-PPO**. **REPPO** exhibits mode collapse because a Gaussian policy can represent only one mode. The diffusion baseline **DPPO** [1] also fails to capture the multimodal action distribution and instead produces cyclic behavior. In contrast, **DIME-REPPO** and our methods **DME-REPPO, DA-MDP WPO, and DME-PPO** cover both reward maxima for each state, resulting in richer behavior. This supports our claim that the proposed framework improves policy expressiveness when multimodality matters.

The toy example also shows that reward alone is insufficient to reveal whether a method captures the full multimodal structure: all methods achieve similar reward, yet diffusion-based methods produce richer behavior, i.e., a higher-entropy trajectory distribution due to better multimodal coverage. We therefore view the practical value of diffusion policies not only in return, but also in their ability to represent richer action distributions than standard unimodal policies.

**A4: Q2 - Q-guided diffusion sampling**  
Following the reviewer’s suggestion, we conducted experiments to evaluate Q-guided diffusion sampling. Results are available at [link](https://github.com/dmerlicml/DMERL_Rebuttal/blob/main/Guidance/Guidance.pdf). To introduce guidance, we incorporated an interpolation parameter, **$\alpha$**, and a step size, **$dt$**, to calibrate the gradient magnitude of the Q-function. When **$\alpha = 1$**, the Q-function serves as the sole control signal. Our findings indicate that, even with a well-calibrated **$dt$**, guidance via the Q-function generally yields inferior results compared to the learned score. We did not observe meaningful improvements for intermediate values of **$\alpha$**, except for *HopperHop*. There, the results suggest that small **$\alpha$** values *might* help, provided **$dt$** is chosen appropriately.

**A5: Q5 - Performance / compute normalization.**  
All results in Figures 1 and 2 are already normalized by the number of **environment interactions**. Compute efficiency is important, and we discuss this runtime/compute trade-off in L.411ff and Appendix Table 1. We also provide per-algorithm runtime on each environment at [link](https://github.com/dmerlicml/DMERL_Rebuttal/blob/main/Table/RuntimeTable.pdf).

We will incorporate these clarifications into the revised manuscript.

[1] Ren, A. Z., et al. *Diffusion Policy Policy Optimization.* ICLR, 2025.


We appologice that we were unable to adress all of the reviewers previous questions due to the character cound restriction: We will answer the reviewer **A1: Role of unnormalized target distribution in L.082-087** Unfortunately, it was for us not possible to answer this question in our first answer due to the character limit. We are happy to adress this question now: The unnormalized target distribution is given by $\widetilde{\pi}(a_{0:T}) = \int_{s_{0:T+1}} \prod_{t=0}^{T} p(s_{t+1}| s_t,a_t)\, \widetilde{\pi}(a_t| s_t)\, p(s_0)\, d s_{0:T+1},$ where $\widetilde{\pi}(a_t| s_t) \propto \exp(\alpha R_\mathrm{env}(s,a))$ which we can thus rewrite to $\widetilde{\pi}(a_{0:T}) = \int_{s_{0:T+1}} \Big ( p(s_0) \prod_{t=0}^{T} p(s_{t+1}| s_t,a_t) \Big ) \exp(\alpha \sum_{t=1}^T R_\mathrm{env}(s_t,a_t))\,\, \, d s_{0:T+1},$. As explained in L0.88 right defines a reward-weighted trajectory distribution, where trajectories with higher cumulative rewards receive exponentially more probability mass. This is because this target distribution is weighted by $\prod_{t=1}^T \widetilde{\pi}(a_t| s_t) = \exp(\alpha \sum_{t=1}^T R_\mathrm{env}(s_t,a_t))$ and not only as the reviewer has stated only by $\exp(\alpha R_\mathrm{env}(s_t,a_t))$. **A2: What is the role of the assumption that $\pi(a|s) = exp(\alpha R_\mathrm{env}(s,a))$** We also want to emphasize that that this is a common assumption when deriving Reinforcement Learning algorithms from a variational inference perspective and is for example done in (see Eq. 8 and the equation above Eq. 11 in (Levine [1]) where $exp(\sum_{t=1}^T r(s_t, a_t))$ is the equivalent to $\prod_{i = 1}^T \pi(a_t|s_t)$, where $\pi(a_t|s_t) = exp(\alpha R_\mathrm{env}(s_t,a_t)$) in our paper). However, as we state in L100 ff right. this unnormalized target distribution is not tracktable which is why we apply the data processing inequality on equation 1, which yields the tracktable reverse Kl divergence between the joint probability distributions of the target and variational distribution L.110-117 left. This KL divergence betweent the joint target and variational distribution is directly equivalent to Eq 11 in Levine et al.. As we argue in 148 left ff. by applying the policy gradient theorem on this KL, we can then derive the surrogate loss function in Eq. 4 which directly aligns with the policy loss in Maximum entropy Reinforcement Learning. In this loss we agree with the reviewer that the optimal policy $q^*(a_t,s_t)$ is indeed proportional to $\exp(\alpha Q(a_t, s_t))$. However, this is the optimal policy for the surrogate loss. We agree with the reviewer that this should be explained more clearly and thank the reviewer for pointing this out. So to summarize the confusion of the reviewer might come from the fact that what we denote as $\pi(a_t|s_t)$ is written in levine at al as p(\mathcal{O_t}|s_t, a_t) (Eq. 3). We do not use $p(\mathcal{O_t}|s_t, a_t) $ as we do not derive the surrogate loss with the optimality arguments as in Levine et al. but derive it by leveraging the Data Processing Inequality, which is then more convenient to derive the Diffusion based Maximum Entropy Reinforcement Learning MDP. A2: Q2: We are confused by the reviewers statement that we claim that our method is more time efficient. We have never claimed this in the paper nor in the rebuttal. We kindly ask the reviewer to tell us where, we claimed to be more efficient in terms of time costs? We are a bit confused by this claim. As the reviewer has also noted we are claiming to be more flexible in terms of memory (see A2 to Reviewer k9mC). We argue in this answer that the inference cost of our loss is O(1) and the memory cost is O(\kappa) but we acknowledge that if $\kappa < K$ we have $K/\kappa$ more update steps and thus there is no time efficiency benefit overall. In the answer to reviewer k9mC we are clearly talking about a memory and runtime compute trade off. Yes in terms of inference the loss in we aregue that our loss is O(1) and in terms of memory it is O(\kappa), but when $\kappa < K$ we of course require $K/\kappa$ more update steps and thus this runtime benefit disappears. Finally we would like to emphasize that we note in L. 422 ff. that the longer runtime is largely attributed to our experimental design. Because here we chose $\kappa = K/2$ in order to demonstrate that we can indeed subsample in terms of diffusion steps via our method. However as stated above this comes with an additional time cost due to the memory and time efficiency trade off. In the following we provide results, where we set $\kappa = K$ and show that this results to a similar runtime for Reppo_DIME and DME-REPPO. We will extend this time comparison for the updated manuscript of the paper. A3: Q3: We believe that we have already made this distinction clear reviewer k9mC